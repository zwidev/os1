{
  "_comment": "OpenClaw gateway config template. Primary model defaults to Qwen2.5-14B. If the optional primary-llm component runs with an Anthropic key, it patches model.primary to Claude Sonnet and enables the anthropic plugin. {{PLACEHOLDERS}} are filled by the ZenCore provisioner. NO real secrets live in this file.",
  "agents": {
    "defaults": {
      "workspace": "/opt/os1/.openclaw/workspace",
      "timeoutSeconds": 60,
      "models": {
        "ollama/qwen2.5:14b": {},
        "anthropic/claude-sonnet-4-6": {}
      },
      "model": {
        "primary": "ollama/qwen2.5:14b"
      }
    }
  },
  "gateway": {
    "mode": "local",
    "auth": {
      "mode": "token",
      "token": "{{GATEWAY_TOKEN}}"
    },
    "port": 18789,
    "bind": "loopback",
    "controlUi": { "allowInsecureAuth": false },
    "http": {
      "endpoints": {
        "chatCompletions": { "enabled": true }
      }
    },
    "nodes": {
      "denyCommands": [
        "camera.snap", "camera.clip", "screen.record",
        "contacts.add", "calendar.add", "reminders.add",
        "sms.send", "sms.search"
      ]
    }
  },
  "memory": {
    "backend": "builtin"
  },
  "tools": {
    "profile": "coding"
  },
  "plugins": {
    "entries": {
      "ollama": {
        "enabled": true,
        "config": {
          "baseUrl": "http://127.0.0.1:11434"
        }
      },
      "anthropic": {
        "enabled": false
      },
      "memory-core": {
        "enabled": true,
        "config": {
          "dreaming": { "enabled": true, "frequency": "0 3 * * *" }
        }
      },
      "memory-wiki": {
        "enabled": true,
        "config": {
          "vault": { "path": "/opt/os1/.openclaw/wiki/main", "renderMode": "obsidian" },
          "obsidian": { "enabled": true, "vaultName": "OS1 Memory" },
          "bridge": { "enabled": true, "indexDailyNotes": true, "indexMemoryRoot": true, "followMemoryEvents": true },
          "render": { "preserveHumanBlocks": true, "createBacklinks": true, "createDashboards": true }
        }
      }
    }
  }
}
