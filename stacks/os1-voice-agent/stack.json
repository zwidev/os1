{
  "schemaVersion": 1,
  "id": "os1-voice-agent",
  "name": "OS1 Voice Agent",
  "version": "0.2.0",
  "summary": "Real-time voice AI. ElevenLabs voice talks to a local Qwen2.5-14B model for fast, private conversation; an OpenClaw agent performs file/code/system tasks in the background; persistent Obsidian-backed memory recalls context across sessions. No cloud LLM API key required.",
  "reviewStatus": "review",
  "deploymentProfile": "openclaw-qwen-elevenlabs-obsidian",
  "components": [
    {
      "id": "agent-gateway",
      "type": "runtime",
      "path": "components/agent-gateway",
      "installOrder": 10,
      "required": true,
      "notes": "OpenClaw gateway (agent/tools host) + the voice proxy that splits fast conversation from background task execution."
    },
    {
      "id": "local-inference",
      "type": "inference",
      "path": "components/local-inference",
      "installOrder": 20,
      "required": true,
      "notes": "Ollama server. Pulls Qwen2.5-14B as the primary conversational LLM and nomic-embed-text for semantic memory recall. No external API key needed."
    },
    {
      "id": "memory-vault",
      "type": "data",
      "path": "components/memory-vault",
      "installOrder": 30,
      "required": true,
      "notes": "OpenClaw builtin memory backend + memory-wiki Obsidian vault + nightly Dreaming consolidation."
    },
    {
      "id": "voice-channel",
      "type": "integration",
      "path": "components/voice-channel",
      "credentialIds": ["elevenlabs_api_key", "elevenlabs_agent_id"],
      "installOrder": 40,
      "required": true,
      "notes": "ElevenLabs Conversational AI agent. Points its custom-llm Server URL at this tenant's gateway."
    },
    {
      "id": "os1-dashboard",
      "type": "dashboard",
      "path": "components/os1-dashboard",
      "installOrder": 50,
      "required": true,
      "notes": "OS1 React UI for starting/holding the voice conversation."
    }
  ],
  "services": [
    { "id": "agent-gateway", "unit": "agent-gateway.service", "componentId": "agent-gateway" },
    { "id": "voice-proxy", "unit": "voice-proxy.service", "componentId": "agent-gateway" }
  ],
  "entrypoints": {
    "setupUrl": "/",
    "dashboardUrl": "/",
    "primaryPort": 18789
  },
  "requiredCredentials": [
    { "id": "elevenlabs_api_key", "label": "ElevenLabs API key", "requiredFor": "voice synthesis and agent configuration" },
    { "id": "elevenlabs_agent_id", "label": "ElevenLabs Agent ID", "requiredFor": "voice channel" }
  ],
  "resourceHints": { "minMemoryMb": 16384, "gpuOptional": true },
  "exportPolicy": {
    "excludeSecrets": true,
    "excludePrivateMemory": true,
    "excludeCustomerData": true
  }
}
